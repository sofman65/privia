# -------------------------------------------------------------------
# Privia backend – environment configuration
# Copy to .env and adjust values for your deployment.
# -------------------------------------------------------------------

# Runtime environment: "development" | "production"
ENV=development

# JWT signing key – MUST be changed in production
SECRET_KEY=change-me-in-production

# SQLite database path (relative to working directory)
DATABASE_URL=sqlite:///./privia.db

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000
# Optional CORS regex for dynamic hosts (example: https://.*\.vercel\.app)
ALLOWED_ORIGIN_REGEX=

# Bind address and port for uvicorn
API_HOST=0.0.0.0
API_PORT=8000
